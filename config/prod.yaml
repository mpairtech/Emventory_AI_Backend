# Production configuration (template)
#
# This file is optional and NOT consumed by the app right now.
# The application uses environment variables via `app/core/config.py`.
#
# Recommended approach:
# - Provide secrets via environment variables / secret manager
# - Avoid committing any real credentials here

app:
  log_level: INFO
  active_provider: gemini

db:
  host: your-db-host
  port: 5432
  name: Search
  user: postgres
  password: change_me

