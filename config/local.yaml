# Local configuration (template)
#
# This file is optional and NOT consumed by the app right now.
# The application uses environment variables via `app/core/config.py`.
#
# Keep real secrets in `.env` (not committed). Use `.env.example` as reference.

app:
  log_level: INFO
  active_provider: gemini

db:
  host: localhost
  port: 5433
  name: Search
  user: postgres
  password: postgres

# Local configuration (template)
#
# This file is optional and NOT consumed by the app right now.
# The application uses environment variables via `app/core/config.py`.
#
# Keep real secrets in `.env` (not committed). Use `.env.example` as reference.

app:
  log_level: INFO
  active_provider: gemini

db:
  host: localhost
  port: 5433
  name: Search
  user: postgres
  password: postgres

